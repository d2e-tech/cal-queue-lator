class QueueJob {
  // The clock time at which the job arrived (or will arrive) in the queue.
  arrivalTime: number;
  // How many units time are required to process this job
  procDuration: number;
}
